import styled from '@emotion/styled';

const StyledSelect = styled.select`
  border: none;
  font-size: 1.2rem;
  padding: 1rem 1rem;
  background-color: #f1f5f9;
  border-radius: 10px;
  outline: none;
`;

export { StyledSelect };
