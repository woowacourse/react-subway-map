import LineListItem from './LineListItem';

export default {
  title: 'LinePage/LineListItem',
  component: LineListItem,
};

const Template = () => <LineListItem name='1호선' />;
export const Default = Template.bind({});
