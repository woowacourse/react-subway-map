import AddLineModalForm from './AddLineModalForm';

export default {
  title: 'LinePage/AddLineModalForm',
  component: AddLineModalForm,
};

const Template = () => <AddLineModalForm />;
export const Default = Template.bind({});
