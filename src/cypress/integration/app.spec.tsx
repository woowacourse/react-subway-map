describe("", () => {
  beforeEach(() => {
    cy.visit("/");
  });

  it("should work", () => {
    /* ==== Generated with Cypress Studio ==== */
    cy.findByText(/👤 로그인/);
    /* ==== End Cypress Studio ==== */
  });
});

export {};
