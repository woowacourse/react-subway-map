import styled from "styled-components";

import { FlexCenter } from "../../../components/Layout/FlexContainer/FlexContainer";
import { SIZE } from "../../../constants/size";

export const LineManagementPageBlock = styled(FlexCenter)`
  max-width: ${SIZE.PAGE_CONTAINER_WIDTH};
`;
